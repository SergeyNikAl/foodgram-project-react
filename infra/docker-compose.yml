version: '3.8'
services:

  db:
    image: postgres:13.0-alpine
    volumes:
      - db:/var/lib/postgresql/data/
    env_file:
      - ./.env

  backend:
    image: sergeynikal/foodgram_backend:latest
    restart: always
    volumes:
      - static_value:/app/static/
      - media_value:/app/media/
    depends_on:
      - db
    env_file:
      - ./.env

  frontend:
    image: sergeynikal/foodgram_frontend:latest
    volumes:
      - ../frontend/:/app/result_build/
    depends_on:
      - db

  nginx:
    image: nginx:1.21.3-alpine
    ports:
      - "443:443"
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
      - ../frontend/build:/usr/share/nginx/html/
      - ../docs/:/usr/share/nginx/html/api/docs/
      - ssl_certificate:/etc/letsencrypt/live/coocking.ddns.net/fullchain.pem;
      - ssl_certificate_key:/etc/letsencrypt/live/coocking.ddns.net/privkey.pem;
      - ssl_certificate_options:/etc/letsencrypt/options-ssl-nginx.conf;
      - ssl_dhparam:/etc/letsencrypt/ssl-dhparams.pem
      - static_value:/var/html/static/
      - media_value:/var/html/media/
    restart: always
    depends_on:
      - backend
      - frontend

volumes:
  ssl_certificate:
  ssl_dhparam:
  ssl_certificate_key:
  ssl_certificate_options:
  db:
  result_build:
  static_value:
  media_value: